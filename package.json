{
  "name": "fishygrambot",
  "version": "0.0.1",
  "description": "a test bot for telegram",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build:live",
    "build:live": "nodemon --exec ./node_modules/.bin/ts-node -- ./bot.ts"
  },
  "author": "stickyfishes",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^8.0.14",
    "nodemon": "^1.11.0",
    "ts-node": "^3.2.0",
    "typescript": "^2.4.1"
  },
  "dependencies": {
    "telebot": "^1.2.2"
  }
}
